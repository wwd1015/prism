```{python}
#| output: asis
#| echo: false
print(report.section_header("rank_ordering"))
```

```{python}
#| output: asis
#| echo: false
print(metric_intro("rank_ordering",
    "The Gini coefficient measures the model's ability to distinguish between "
    "events and non-events. It is derived from the Lorenz curve — a higher "
    "Gini indicates stronger discriminatory power. A value of 0 means no "
    "separation (random model), while 1 indicates perfect separation."
))
```

```{python}
#| echo: false
result = report.metric("rank_ordering")
fig = result.get("lorenz_chart")
if fig:
    show_chart(fig)
```

```{python}
#| output: asis
#| echo: false
print(report.commentary("rank_ordering"))
```
